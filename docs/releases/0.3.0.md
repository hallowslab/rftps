## rftps v0.3.0

- **FTPS improvements**: certificate/key file support and TLS enablement
- Raised the default port to a higher range (lower ports can be blocked on some systems)
- Utility and validation fixes with added tests
- Removed the unused static feature flag; code cleanup